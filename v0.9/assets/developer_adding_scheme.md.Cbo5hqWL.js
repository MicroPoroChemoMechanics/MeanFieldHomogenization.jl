import{_ as o,o as t,c as n,ao as i}from"./chunks/framework.BcYWIbHq.js";const p=JSON.parse('{"title":"Adding a homogenization scheme","description":"","frontmatter":{},"headers":[],"relativePath":"developer/adding_scheme.md","filePath":"developer/adding_scheme.md","lastUpdated":null}'),a={name:"developer/adding_scheme.md"};function c(s,e,d,l,r,h){return t(),n("div",null,[...e[0]||(e[0]=[i('<h1 id="dev-adding-scheme" tabindex="-1">Adding a homogenization scheme <a class="header-anchor" href="#dev-adding-scheme" aria-label="Permalink to &quot;Adding a homogenization scheme {#dev-adding-scheme}&quot;">​</a></h1><div class="tip custom-block"><p class="custom-block-title">Which cell does your scheme serve?</p><p><code>homogenize</code> accepts any <a href="/MeanFieldHomogenization.jl/v0.9/api/laminate#MeanFieldHomogenization.Core.AbstractHomogenizationCell"><code>AbstractHomogenizationCell</code></a> — an <a href="/MeanFieldHomogenization.jl/v0.9/api/schemes#MeanFieldHomogenization.Schemes.RVE"><code>RVE</code></a> or a <code>Laminate</code>. Schemes stay typed on the <strong>concrete</strong> cell they serve (<code>_evaluate(rve::RVE, ::MyScheme, …)</code>), so a scheme applied to a cell it does not serve falls through to the error-raising fallback instead of dispatching somewhere wrong. Type the <em>scheme</em> argument too: leaving it untyped is ambiguous with that fallback.</p></div><p><code>MeanFieldHomogenization.Schemes</code> ships Voigt/Reuss, dilute (direct and dual), Mori-Tanaka, Maxwell, Ponte-Castañeda–Willis, self-consistent (symmetric and asymmetric) and differential, each in an elastic and an ageing-viscoelastic flavor. A new scheme slots in beside them:</p><ol><li><p>Create <code>src/Schemes/&lt;scheme_name&gt;.jl</code> and <code>include</code> it from <code>src/Schemes/Schemes.jl</code>.</p></li><li><p>Declare the scheme type (<code>struct MyScheme &lt;: HomogenizationScheme</code>) in <code>src/Schemes/scheme_types.jl</code>, and register its symbol alias in <code>SCHEME_ALIAS</code> (<code>src/Schemes/homogenize.jl</code>) if you want <code>homogenize(rve, :myscheme, :C)</code> to work.</p></li><li><p>Implement <code>_evaluate(rve, ::MyScheme, ::Val{p}; kw...)</code>. Provide <strong>one method per tensor order</strong> — dispatch on the order of <code>reference_property(rve, scheme, p)</code> — so the scheme serves elasticity and transport from one implementation, as <code>_mt_dispatch</code> does in <code>mori_tanaka.jl</code>.</p></li><li><p><strong>Go through <code>contribution_helpers.jl</code>.</strong> Never call <code>stiffness_contribution</code> and friends directly on a phase geometry: the <code>_phase_*</code> helpers apply the amount, honor the <code>symmetrize</code> setting, hand the kernel the correctly pre-projected reference medium, and branch on <a href="/MeanFieldHomogenization.jl/v0.9/api/localization#MeanFieldHomogenization.Core.is_homogeneous_inclusion"><code>is_homogeneous_inclusion</code></a>. Two invariants stated in that file&#39;s header must hold in your kernel too — all helpers of a given evaluation must share the same projected <code>P₀</code>, and orientation averaging must never be folded into a tensor product.</p></li><li><p>Prefer the bundled seams (<code>_phase_dilute_and_contribution</code>, <code>_phase_dilute_and_stress_average</code>, <code>_phase_compliance_and_contribution</code>) when you need two objects per phase: they share the single expensive solve.</p></li><li><p>Consider what your kernel <em>requires</em> of an inclusion. Needing only the contribution tensors keeps the scheme usable with inclusions entered through gate C of the <a href="/MeanFieldHomogenization.jl/v0.9/developer/adding_inclusion#dev-adding-inclusion">inclusion contract</a>; needing the concentration tensor restricts it.</p></li><li><p>Export through <code>src/Schemes/Schemes.jl</code> and re-export from <code>src/MeanFieldHomogenization.jl</code>.</p></li><li><p>Add a unit test under <code>test/Schemes/</code> and a section in <code>docs/src/manual/schemes.md</code>.</p></li></ol><div class="tip custom-block"><p class="custom-block-title">A scheme need not act on an RVE</p><p>Two cells other than <code>RVE</code> carry scheme kernels, and both follow the same split: the scheme <strong>type</strong> is declared in <code>src/Schemes/scheme_types.jl</code> (so the hierarchy and <code>SCHEME_ALIAS</code> stay in one place) while its <code>_evaluate</code> method lives with the cell it acts on — <code>Laminated</code> in <code>src/Laminates/</code>, and the two N-body schemes <a href="/MeanFieldHomogenization.jl/v0.9/api/assemblies#MeanFieldHomogenization.Schemes.ClusterModel"><code>ClusterModel</code></a> / <a href="/MeanFieldHomogenization.jl/v0.9/api/assemblies#MeanFieldHomogenization.Schemes.EquivalentInclusion"><code>EquivalentInclusion</code></a> in <code>src/Assemblies/</code>. If your scheme needs microstructural information an <code>RVE</code> does not carry — positions, in the N-body case — add a cell rather than widening <code>RVE</code>, and remember that MFH Studio discovers every concrete <code>HomogenizationScheme</code> automatically: a scheme the interface cannot drive must be listed in <code>ASSEMBLY_SCHEMES</code> (<code>tools/mfhstudio/mfhstudio/model.py</code>) so it is filtered out of its catalog.</p></div>',5)])])}const u=o(a,[["render",c]]);export{p as __pageData,u as default};
